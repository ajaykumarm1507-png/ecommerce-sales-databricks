Place the supplied files in `data/source/` for local reference:

- `Customer.xlsx`
- `Products.csv`
- `Orders.json`

The files are intentionally excluded from Git because the assignment provides them through a Drive link. The downloadable project bundle includes the files for convenience.
