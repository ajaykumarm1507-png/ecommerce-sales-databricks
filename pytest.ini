[pytest]
testpaths = tests
pythonpath = src
addopts = -ra --strict-markers
markers =
    unit: fast tests for isolated transformation functions
    data_quality: tests for validation and bad-record handling
    integration: tests covering multiple transformations together
